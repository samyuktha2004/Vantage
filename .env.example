# ─── Supabase ─────────────────────────────────────────────────────────────────
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here

# ─── Database ─────────────────────────────────────────────────────────────────
# Get from: Supabase project → Settings → Database → Connection string → URI
# Use the direct connection string (not the pooler/session mode one)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# ─── Session ──────────────────────────────────────────────────────────────────
# Generate a random secret: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
SESSION_SECRET=replace-this-with-a-random-64-char-hex-string

# ─── Server Port ──────────────────────────────────────────────────────────────
# Default is 5000. macOS Monterey+ uses 5000 for AirPlay — set to 5001:
PORT=5001

# ─── TBO Hotel API (B2B Holidays) ─────────────────────────────────────────────
TBO_HOTEL_URL=http://api.tbotechnology.in/TBOHolidays_HotelAPI
TBO_HOTEL_USERNAME=
TBO_HOTEL_PASSWORD=

# ─── TBO Air API (TekTravels UAT) ─────────────────────────────────────────────
TBO_AIR_URL=https://api.tektravels.com
TBO_AIR_USERNAME=
TBO_AIR_PASSWORD=
TBO_AIR_SERVER_IP=127.0.0.1
# Auth lives on a different domain from the main booking API — override only if needed
# TBO_AIR_AUTH_URL=https://Sharedapi.tektravels.com/SharedData.svc/rest
# Airline Sector List uses a different base URL (2025 release) — override only if needed
# TBO_AIR_SECTOR_URL=https://tboapi.travelboutiqueonline.com/AirAPI_V10/AirService.svc/rest
